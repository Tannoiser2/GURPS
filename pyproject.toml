[project]
name = "gurps"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "anthropic",
    "google-genai",
    "openai",
    "python-multipart",
    "python-dotenv",
]
